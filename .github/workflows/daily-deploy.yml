name: daily-deploy

on:
  schedule:
    - cron: "0 12 * * *"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel rebuild
        run: curl -s -X POST "${{ secrets.VERCEL_DEPLOY_HOOK_URL }}" | tee /dev/stderr
